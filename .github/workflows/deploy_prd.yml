name: Deploy Databricks Bundle PRD

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy-prd:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Instalar Databricks CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | bash

      - name: Instalar uv
        run: |
          curl -Ls https://astral.sh/uv/install.sh | bash

      - name: Configurar credenciais Databricks (OAuth Service Principal)
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST_PRD }}
          DATABRICKS_CLIENT_ID: ${{ secrets.DATABRICKS_CLIENT_ID_PRD }}
          DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_CLIENT_SECRET_PRD }}
        run: |
          mkdir -p ~/.databricks
          echo "[PRD]" > ~/.databrickscfg
          echo "host = $DATABRICKS_HOST" >> ~/.databrickscfg
          echo "client_id = $DATABRICKS_CLIENT_ID" >> ~/.databrickscfg
          echo "client_secret = $DATABRICKS_CLIENT_SECRET" >> ~/.databrickscfg

      - name: Validar bundle Databricks
        env:
          DATABRICKS_CONFIG_PROFILE: PRD
        run: |
          cd dab_test
          databricks bundle validate --target prd

      - name: Deploy bundle Databricks
        env:
          DATABRICKS_CONFIG_PROFILE: PRD
        run: |
          cd dab_test
          databricks bundle deploy --target prd

      - name: Executar job Databricks
        env:
          DATABRICKS_CONFIG_PROFILE: PRD
        run: |
          cd dab_test
          databricks bundle run dab_test_job --target prd